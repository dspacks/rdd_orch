import sqlite3
import sys

print(f"Python: {sys.version}")
print(f"SQLite3 module version: {sqlite3.version}")
print(f"SQLite3 library version: {sqlite3.sqlite_version}")
print("SQLite3 is correctly installed and available.")
